import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'chapeu_seletor',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
